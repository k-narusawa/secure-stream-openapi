patch:
  operationId: requestChangeLoginId
  tags: 
    - login_id
  summary: 変更
  description: ログインIDの変更を行うためのリクエストです。送信されたメールアドレスに変更用のURLが送信されます。
  security:
    - Bearer: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../../../components/schemas/requestBodies/ChangePassword.yml'
  responses:
    '204':
      description: ログインID変更成功
