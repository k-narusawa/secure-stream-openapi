type: object
required:
  - error
properties:
  error:
    type: object
    required:
      - request_id
      - code
      - message
    properties:
      request_id:
        type: string
        description: "リクエストID"
      code: 
        type: string
        enum: 
          - "ID401E001"
        description: |
          エラーコード
          | エラーコード | 説明 |
          |-|-|
          | ID401E001 | アクセストークン期限切れ |
      message:
        type: string
        description: "エラーメッセージ"